@model IEnumerable<Data.Models.SpecialDivision>

@{
    ViewData["Title"] = "Index";
}

<h1>Special Divisions</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Leader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Leader.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SDId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SDId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SDId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
